These files are used for activation and enabling YouTube on iPhone.

activation.zip is just the zipped version of all the subdirectories in this directory. It is referred to inside the Payload source when it must be downloaded and extracted.

It is basically a tree that is to be merged with the main iPhone/iPod touch filesystem. The Payload executable takes care of setting the permissions.


This feature has not been extensively tested.
